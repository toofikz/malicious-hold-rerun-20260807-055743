name: owner-authored baseline

on:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  baseline:
    runs-on: ubuntu-latest
    steps:
      - run: printf 'OWNER_BASELINE_SAFE\n'
